{
  "name": "betting-pools",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ betting-pools",
    "create-local": "graph create --node http://localhost:8020/ betting-pools",
    "remove-local": "graph remove --node http://localhost:8020/ betting-pools",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 betting-pools",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.96.0",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
